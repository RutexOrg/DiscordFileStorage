import { createClient } from "webdav"
import { assert } from "chai";
import { describe, it } from "mocha";

describe("Initial mocha test", () => {
	it("Initial mocha test", async () => {
		assert.equal(5, 5);
	});
});